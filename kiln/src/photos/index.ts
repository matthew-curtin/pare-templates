import buckets from "./buckets.jpg";
import elements from "./elements.jpg";
import greenShelf from "./green-shelf.jpg";
import stacked from "./stacked.jpg";

/**
 * Four photographs, imported once.
 *
 * Every one of them goes through `components/plate.tsx`, which is the
 * only file in this template allowed to render an image — see the note
 * there and CONVENTIONS §6.
 */
export const photos = { buckets, elements, greenShelf, stacked };
